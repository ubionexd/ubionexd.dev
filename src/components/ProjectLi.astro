---
export interface Props {
    name: String;
    link: String;
    description: String;
}

const { name, link, description } = Astro.props;
---

<li><a class="text-link duration-300 hover:text-hlink" target="_blank" href={link}>{name}</a> - {description}</li>